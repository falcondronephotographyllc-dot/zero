[project]
name = "project01-coordinator"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2",
  "redis>=5",
  "httpx>=0.26",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
